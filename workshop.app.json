{
  "name": "control-panel",
  "services": [
    {
      "name": "web",
      "cwd": ".",
      "build": "test -d .venv || python3 -m venv .venv; ./.venv/bin/python -m pip --version >/dev/null 2>&1 || ./.venv/bin/python -m ensurepip --upgrade; ./.venv/bin/python -m pip install -r requirements.txt",
      "start": "./.venv/bin/python -m web.app --host 127.0.0.1 --port $PORT --no-browser",
      "port_env": "PORT",
      "health": "/api/metrics",
      "expose": true,
      "env": {
        "PYTHONPATH": "src",
        "XDG_RUNTIME_DIR": "/run/user/1001",
        "DBUS_SESSION_BUS_ADDRESS": "unix:path=/run/user/1001/bus"
      }
    }
  ]
}
